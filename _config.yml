url: https://ruthgervdv.github.io
baseurl: /portfolio

collections:
  markers:
    permalink: /storelocator/:title
    output: true

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "markers"
    values:
      layout: "storelocation"